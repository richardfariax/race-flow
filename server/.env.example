# Porta do servidor (Render injeta PORT automaticamente)
PORT=2567

# Supabase — OPCIONAL. Sem estas vars o jogo funciona, mas sem persistência
# de moedas/resultados (modo convidado). NUNCA commitar valores reais.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
