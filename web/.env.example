# Todas OPCIONAIS em dev local (defaults: servidor local, modo convidado).
# NUNCA commitar valores reais.

# WebSocket do servidor Colyseus (Render em produção, ex: wss://race-flow.onrender.com)
VITE_GAME_SERVER_URL=ws://localhost:2567

# Supabase (contas/economia). Sem estas vars: só modo convidado, sem persistência.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
